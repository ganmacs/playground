#include <algorithm>
#include <iostream>
#include <sstream>
#include <string>
#include <vector>
#include <cstdio>
#include <cmath>
#include <queue>
#include <map>

using namespace std;

int main()
{
  long long A, B;
  cin >> A >> B;
  long long l = B - A + 1;



  // vector<int> V;
  // while (t) {
    // V.push_back(t%2 == 1);
    // t /= 2;
  // }

  // reverse(V.begin(), V.end());

  // for (auto& vi: V) {
    // printf("%d\n", vi);
  // }

  return 0;
}
