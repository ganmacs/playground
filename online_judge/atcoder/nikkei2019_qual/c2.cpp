#include <algorithm>
#include <iostream>
#include <sstream>
#include <string>
#include <vector>
#include <cstdio>
#include <cmath>
#include <queue>
#include <map>
#include <set>

using namespace std;

int main()
{
  int N, M, K, a, b;
  cin >> N >> M >> K;
  map<int, set<int>> A;

  for (int i = 0; i < N; i++) {
    cin >> a >> b;
    A[a].insert(A)

  }


 return 0;
}
