#include <algorithm>
#include <iostream>
#include <sstream>
#include <string>
#include <vector>
#include <cstdio>
#include <cmath>
#include <queue>
#include <stack>
#include <map>
#include <set>

using namespace std;

long long W;
int N;
int dp[210][210], v[210], w[210];

int main(int argc, char *argv[])
{
  cin >> N >> W;
  for (int i = 0; i < N; i++) {
    cin >> v[i + 1] >> w[i + 1];
  }

  for (int i = 0; i < W; i++) {

  }
}
