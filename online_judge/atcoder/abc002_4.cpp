#include <algorithm>
#include <iostream>
#include <sstream>
#include <string>
#include <vector>
#include <cstdio>
#include <cmath>
#include <queue>
#include <stack>
#include <map>
#include <set>

using namespace std;

int main(int argc, char *argv[])
{
  int N, M, x[10000], y[10000];
  // cin >> N >> M;

  for (int i = 0; i < M; i++) {
    cin >> x[i] >> y[i];
  }

  for (long long i = 0; i < (1LL << M); i++) {
    for (int j = 0; j < M; j++) {
      if ((1 << j)) {
      }
    }
  }
}
