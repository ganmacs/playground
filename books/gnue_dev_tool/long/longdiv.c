#include <stdio.h>

unsigned long long a = 12340000, b = 10000;

int main() {
  printf("sizeof(unsigned logn long) is %d bytes\n", sizeof(unsigned long long));
  printf("a / b = %ld\n", a / b);
  return 0;
}


